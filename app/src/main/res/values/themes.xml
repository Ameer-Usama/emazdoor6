<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EMazdoor6" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- Ensure buttons can display custom backgrounds -->
        <item name="android:buttonStyle">@style/CustomButtonStyle</item>
    </style>

    <style name="Theme.EMazdoor6" parent="Base.Theme.EMazdoor6" />
    
    <!-- Custom button style to ensure proper rendering of backgrounds and drawables -->
    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:background">@null</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>